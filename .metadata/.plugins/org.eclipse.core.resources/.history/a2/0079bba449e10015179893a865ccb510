import UDPClient;
public class Test {

	public static void main(String[] args) {

		UDPClient client = new UDPClient();
		byte[] bytes = new byte[6 ];
		for (int i = 0; i < 6; i++) {
			bytes[i] = (byte) 0xff; }
		client.send(bytes);
	}

}
